apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: builderhub
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: builderhub
      owner: builderhub
      secret:
        name: builderhub-db
